import React from "react";
import Button from "@mui/material/Button";

export default function ReactButton() {
    return <Button variant="contained" color='primary' size={'medium'}>
        <span>Create new client</span>
    </Button>
}
